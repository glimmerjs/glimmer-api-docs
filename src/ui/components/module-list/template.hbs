<div>
  <strong>{{@title}}</strong>

  <ul class="project-list">
    {{#each @modules key="@index" as |module|}}
      <li onclick={{action @showModule project module.slug}}>{{module.name}}</li>
    {{/each}}
  </ul>
</div>
